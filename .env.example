# The ID of your GitHub App
APP_ID=
WEBHOOK_SECRET=development

# Use `trace` to get verbose logging or `info` to show less
LOG_LEVEL=debug

# Go to https://smee.io/new set this to the URL that you are redirected to.
WEBHOOK_PROXY_URL=

# Aider Configuration
# API keys for LLM services (required for Aider)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Label that triggers Aider to fix an issue
AIDER_TRIGGER_LABEL=aider:fix
# Additional labels that can trigger Aider (comma-separated)
AIDER_ADDITIONAL_TRIGGER_LABELS=

# Timeouts
AIDER_TIMEOUT=300000
GIT_TIMEOUT=60000

# Labels to add to created pull requests (comma-separated)
AIDER_PR_LABELS=automated-fix,aider-generated

# Comment templates
AIDER_PROCESSING_COMMENT=🤖 I'm working on fixing this issue using Aider...
AIDER_SUCCESS_COMMENT=✅ I've created a pull request with a fix for this issue.
AIDER_FAILURE_COMMENT=❌ I wasn't able to automatically fix this issue with Aider.

# Aider model to use
# For OpenAI models:
AIDER_MODEL=gpt-4o
# For Anthropic/Claude models (MUST use --use-anthropic flag):
# AIDER_MODEL=claude-3-7-sonnet-20250219
# AIDER_MODEL=claude-3-opus-20240229
# AIDER_MODEL=claude-3-sonnet-20240229

# Additional arguments to pass to Aider (comma-separated)
# IMPORTANT: When using Claude models, you MUST include --use-anthropic here
# AIDER_EXTRA_ARGS=--use-anthropic

# Branch configuration
AIDER_BRANCH_PREFIX=aider-fix-

# Pull request configuration
AIDER_PR_TITLE_PREFIX=Fix: 
AIDER_PR_DRAFT=false

# Repository configuration
# Files to include in Aider analysis (glob patterns, comma-separated)
AIDER_INCLUDE_FILES=
# Files to exclude from Aider analysis (glob patterns, comma-separated)
AIDER_EXCLUDE_FILES=node_modules/**,.git/**
