declare module '@octokit/plugin-rest-endpoint-methods/dist-types/generated/method-types' {
  export interface RestEndpointMethods {
    // This is just a placeholder, we're not using this directly anyway
  }
}
